=> Collection  --- is a framework that allows us to store and manipulate the group of objects.
                    (searching, sorting, insertion, deletion)
=> Collection --- is a single unit of objects.
Java Collection framework provides many interfaces (Set, List, Queue, Map).
And classes (ArrayList, Vector, LinkedList, PriorityQueue, HashSet, LinkedHashSet, TreeSet).


